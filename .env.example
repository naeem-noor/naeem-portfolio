# Public site URL used for metadata, Open Graph tags, and canonical links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Social / contact links surfaced in the Navbar and Footer.
NEXT_PUBLIC_GITHUB=https://github.com/naeem-noor
NEXT_PUBLIC_LINKEDIN=https://linkedin.com/in/naeem-noor
NEXT_PUBLIC_EMAIL=naeem.noor03@gmail.com

# Optional. Raises the GitHub REST API's unauthenticated rate limit
# (60/hour per IP) to 5,000/hour for the /projects page's live stats.
# GITHUB_TOKEN=

# Optional, not yet used. Set this (and wire up the corresponding branch
# in app/api/contact/route.ts) to enable real contact-form email delivery
# via Resend. Until then, the contact form honestly reports that
# submissions are received but not emailed — see app/api/contact/route.ts.
